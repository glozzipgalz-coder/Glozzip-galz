<!--
Place this file (glozzip-galz-logo-page.html) in the root of your GitHub repository.
Add the logo image to the same folder and name it: glozzip-galz-logo.jfif
Then enable GitHub Pages (Settings → Pages → Source: main branch / root) to serve it as a static page.
-->
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Glozzip-galz — Logo</title>

  <!-- Google font for a classy look -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --maroon: #5a0f0f;
      --maroon-deep: #4a0b0b;
      --gold: #d9b86a;
      --glass: rgba(255,255,255,0.06);
      --max-width: 900px;
    }

    html,body{height:100%;}
    body{
      margin:0;
      font-family: 'Playfair Display', serif;
      background: linear-gradient(180deg,var(--maroon) 0%, var(--maroon-deep) 100%);
      color:var(--gold);
      display:flex;
      align-items:center;
      justify-content:center;
      padding:32px;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    .container{
      width:100%;
      max-width:var(--max-width);
      text-align:center;
    }

    .card{
      background: linear-gradient(180deg, rgba(0,0,0,0.18), rgba(0,0,0,0.12));
      border-radius:18px;
      padding:28px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.5);
      border: 1px solid rgba(255,255,255,0.03);
    }

    .logo-wrap{
      display:inline-block;
      background: radial-gradient(circle at 30% 20%, rgba(255,255,255,0.02), transparent 20%), var(--maroon-deep);
      padding:18px;
      border-radius:999px;
      box-shadow: inset 0 3px 8px rgba(0,0,0,0.6), 0 8px 30px rgba(0,0,0,0.6);
    }

    .logo{
      display:block;
      width: clamp(200px, 40vw, 420px);
      height:auto;
      border-radius:999px;
      box-shadow: 0 6px 18px rgba(0,0,0,0.45), 0 0 0 6px rgba(217,184,106,0.06);
      border: 6px solid rgba(217,184,106,0.12);
      background-color: transparent;
    }

    h1{
      margin:20px 0 6px 0;
      font-size: clamp(18px, 2.2vw, 28px);
      color:var(--gold);
      letter-spacing:0.6px;
    }

    p.lead{
      margin:0 0 18px 0;
      font-size:14px;
      color:rgba(217,184,106,0.92);
    }

    .controls{
      display:flex;
      gap:12px;
      justify-content:center;
      flex-wrap:wrap;
      margin-top:12px;
    }

    a.btn{
      text-decoration:none;
      padding:10px 16px;
      border-radius:10px;
      background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(0,0,0,0.12));
      color:var(--gold);
      border:1px solid rgba(217,184,106,0.14);
      box-shadow: 0 6px 16px rgba(0,0,0,0.4);
      font-weight:600;
    }

    a.btn.secondary{
      opacity:0.86;
      font-weight:500;
      padding:10px 14px;
    }

    footer{
      margin-top:18px;
      font-size:12px;
      color: rgba(217,184,106,0.66);
    }

    /* Responsive tweaks */
    @media (max-width:520px){
      .card{padding:18px}
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <div class="logo-wrap">
        <!-- Replace the filename below with the exact name you used in the repo -->
        <img class="logo" src="glozzip-galz-logo.jfif" alt="Glozzip-galz logo" />
      </div>

      <h1>Glozzip-galz</h1>
      <p class="lead">Luxury lip gloss — logo preview. Place the image file (<code>glozzip-galz-logo.jfif</code>) in the same repository folder as this HTML file.</p>

      <div class="controls">
        <a class="btn" href="glozzip-galz-logo.jfif" download>Download image</a>
        <a class="btn secondary" href="#" onclick="openRaw()">Open raw image</a>
      </div>

      <footer>
        Tip: enable GitHub Pages for this repository (Settings → Pages) to serve this file at <em>https://&lt;username&gt;.github.io/&lt;repo&gt;/glozzip-galz-logo-page.html</em>
      </footer>
    </div>
  </div>

  <script>
    function openRaw(){
      // Try to open the image in a new tab — when hosted on GitHub Pages the relative path will work.
      const path = 'glozzip-galz-logo.jfif';
      window.open(path, '_blank');
    }
  </script>
</body>
</html>
